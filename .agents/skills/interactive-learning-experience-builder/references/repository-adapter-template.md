# [Repository name] learning-experience adapter

Use this thin adapter only for recurring local workflows. Replace bracketed
placeholders; remove sections that do not represent stable constraints.

## Local context

- Read: `[repository instructions path]`
- Authoritative sources: `[source inventory or ordering]`
- Excluded material: `[private or restricted content rules]`
- Existing assets and data: `[paths or conventions]`

## Recurring workflow

1. Resolve `[local topic or content selector]` using `[local index or rule]`.
2. Ground payload claims in `[approved source order]`.
3. Store content at `[payload convention]` and output HTML at `[output convention]`.
4. Run `[repository validation commands]` and publish through `[publication convention]`.

## Adapter boundary

Do not duplicate the portable content contract, generation implementation, or
offline accessibility checks here. Add only stable local constraints that make
the recurring workflow safer or easier to discover.
